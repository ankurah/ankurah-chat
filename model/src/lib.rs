//! Chat data model (scaffold).
//!
//! The collections arrive as a copy of community's `model` crate — kept
//! collection- and wire-compatible with the community server (see the
//! workspace README's interop constraint). Empty until the copy-out lands.
